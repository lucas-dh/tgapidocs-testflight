# 模型型号操作

## 目录

- [1.连接资产服务器](#1连接资产服务器)
- [2.断开资产服务器链接](#2断开资产服务器链接)
- [3.添加模型型号](#3添加模型型号)
- [4.更新模型型号样式](#4更新模型型号样式)
- [5.删除模型型号](#5删除模型型号)
- [6.添加模型型号地标图](#6添加模型型号地标图)
- [7.更新模型型号地标图数据](#7更新模型型号地标图数据)
- [8.更新模型型号地标图样式](#8更新模型型号地标图样式)
- [9.删除模型型号地标图](#9删除模型型号地标图)
- [10.添加模型型号轨迹图](#10添加模型型号轨迹图)
- [11.更新模型型号轨迹图数据](#11更新模型型号轨迹图数据)
- [12.更新模型型号轨迹图样式](#12更新模型型号轨迹图样式)
- [13.删除模型型号轨迹图](#13删除模型型号轨迹图)

## 1.连接资产服务器

### 功能描述

连接资产服务器

### 调用方法

```js
let jsonData ={  
    name: "serverName"
    address: "//cimserver.com/cim/webapi/v1/resource/",
    token: "ABCDEFG",
}


appInstance.uniCall('connectAssetServer', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| name                   | String  |  资产服务器名称                                                                                                                    |
| address                   | String  |  资产服务器地址 例如: http://ip:port/partNumber/{iconModelId}                                                                                                                    |
| token            | String  | token                                      |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 2.断开资产服务器链接

### 功能描述

断开资产服务器链接

### 调用方法

```js
let jsonData ={  
    name: "serverName"
}


appInstance.uniCall('disconnectAssetServer', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| name                   | String  |  资产服务器名称                                                                                                                    |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 3.添加模型型号

### 功能描述

添加模型型号

### 调用方法

```js
let jsonData ={   
    id: "iconModelId",
    serverName:"serverName"，
    coordType: 0,
    coordTypeZ: 0,
    coord: [114.560257, 37.09079],
    coordZ: 10,
    rotation: [20,80,90],
    alpha: 0.5,
    scale: 25,
    behavior: "normal",
    labelText: “地标”,
    labelScale: 1,
    iconScale: 1
    iconModelId: "1234"
}

appInstance.uniCall('addIconModel', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| serverName                   | String  |  连接的资产服务器的名称                                                                                                                    |
| coordType            | Number  | xy 轴坐标模式 坐标类型(0:x 经度 y 纬度；1:x 米 y 米，相对世界 00 点) 目前该属性仅支持属性值为 0                                      |
| coordTypeZ           | Number  | z 轴坐标模式 坐标高度类型，单位：米(0:相对 3D 世界 0 海拔；1:相对 3D 世界地表；2:相对 3D 模型表面) 目前该属性仅支持属性值为 0        |
| coord                | Array   | XY 轴坐标，X：经度；Y：纬度                                                                                                          |
| coordZ               | Number  | Z 轴高度（单位：米）                                                                                                                 |
| rotation             | Array   | XYZ 三轴旋转度数 [偏航角（单位：角度，0 正北, -360-360）, 俯仰角（单位：角度，0 水平, -360-360）,翻滚角（单位：角度，0 水平,-360-360）] |
| alpha                | Number  | 透明度，0：完全透明；1：完全不透明                                                                                                   |
| scale                | Number  | 模型的放缩倍数（单位：倍），支持单一数值，或者[scaleX, scaleY, scaleZ]的写法。（不包括图标）                                                                                                         |
| behavior            | String  | 模型型号的行为，"normal"：默认行为，无交互，"toggle"：在模型可见距离内，显示标签，隐藏模型，鼠标悬浮，显示半透明模型，和黄色包围盒，单击，一直显示模型，再次单击，隐藏模型                                                                                                                         |
| labelText           | String  | 文字标签内容                                                                                                   |
| labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| iconScale              | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |
| iconModelId            | String  | 模型型号Id                                                                                                                            |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 4.更新模型型号样式

### 功能描述

更新模型型号样式

### 调用方法

```js
let jsonData ={   
    id: "iconModelId",
    alpha: 0.5,
    behavior: "normal",
    labelText: “地标”,
    labelScale: 1,
    iconScale: 1
    iconModelId: "1234"
}


appInstance.uniCall('updateIconModelStyle', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| alpha                | Number  | 透明度，0：完全透明；1：完全不透明                                                                                                   |
| behavior            | String  | 模型型号的行为，"normal"：默认行为，无交互，"toggle"：在模型可见距离内，显示标签，隐藏模型，鼠标悬浮，显示半透明模型，和黄色包围盒，单击，一直显示模型，再次单击，隐藏模型                                                                                                                         |
| labelText           | String  | 文字标签内容                                                                                                   |
| labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| iconScale              | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |
| iconModelId            | String  | 模型型号Id，如果更新，可以变换模型                                                                                                                            |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 5.删除模型型号

### 功能描述

删除模型型号

### 调用方法

```js
let jsonData = {
  id: 'iconModelId',
};
appInstance.uniCall('removeModel', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性        | 类型   | 说明                                                                |
| ----------- | ------ | ------------------------------------------------------------------- |
| id          | String | 模型型号 ID |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

## 6.添加模型型号地标图

### 功能描述

添加模型型号地标图

### 调用方法

```js
let jsonData ={   
    id: "iconModelLandmarkLayerId",
    serverName:"serverName"，
    coordType: 0,
    coordTypeZ: 0,
    snapSurface: 0,
    alpha: 0.5,
    visible: true,
    legends: [
        {
           name: "legendName1",
           iconModelId:"1111",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName2",
           iconModelId:"2222",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName3",
           iconModelId:"3333",
           labelScale: 1,
           iconScale: 1
        }
    ],
    data: [
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        }
    ]
}


appInstance.uniCall('addIconModelLandmarkLayer', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号地标图层 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| serverName                   | String  |  连接的资产服务器的名称                                                                                                                    |
| coordType            | Number  | xy 轴坐标模式 坐标类型(0:x 经度 y 纬度；1:x 米 y 米，相对世界 00 点) 目前该属性仅支持属性值为 0                                      |
| coordTypeZ           | Number  | z 轴坐标模式 坐标高度类型，单位：米(0:相对 3D 世界 0 海拔；1:相对 3D 世界地表；2:相对 3D 模型表面) 目前该属性仅支持属性值为 0        |
| snapSurface                | Number   | 启用自动贴地,0：不贴地；1：贴地                                                                                                          |
| alpha                | Number  | 透明度，0：完全透明；1：完全不透明                                                                                                   |
| visible                | Boolean  | 添加当前图层时默认显示还是隐藏                                                                                                   |
| legends                | Array  | 定义图层包含图例，支持为不同图例选取不同模型型号                                                                                                         |
| legends[0].name            | String  | 图层内图例名称，新建时调用者自己传入的唯一名称，用于各种操作的图例识别                                                                                                                         |
| legends[0].iconModelId           | String  |模型型号Id                                                                                                   |
| legends[0].labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| legends[0].iconScale       | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |
| data       | Array | 定义图层可视化数据                |
| data[0].id       | String |  模型型号对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                |
| data[0].label       | String |  标签文本                |
| data[0].coord       | Array |  XY 轴坐标，X：经度；Y：纬度                |
| data[0].coordZ       | Number |  Z 轴高度（单位：米）                |
| data[0].scale       | Array |  XYZ三轴的放缩倍数（单位：倍），不包含图标                |
| data[0].rotation       | Array |  XYZ 三轴旋转度数 [ 偏航角（单位：角度，0正北, -360 ~ 360）, 俯仰角（单位：角度，0水平, -360 ~ 360）,翻滚角（单位：角度，0水平, -360 ~ 360）]                |
| data[0].type       | String | 模型型号地标图例类别                |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 7.更新模型型号地标图数据

### 功能描述

更新模型型号地标图数据

### 调用方法

```js
let jsonData ={   
    id: "iconModelIdarkLayerId",
    coordType: 0,
    coordTypeZ: 0,
    isAppend: true,
    data: [
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        }
    ]
}


appInstance.uniCall('updateIconModelLandmarkLayerCoord', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号地标对象图层 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                   |
| coordType            | Number  | xy 轴坐标模式 坐标类型(0:x 经度 y 纬度；1:x 米 y 米，相对世界 00 点) 目前该属性仅支持属性值为 0                                      |
| coordTypeZ           | Number  | z 轴坐标模式 坐标高度类型，单位：米(0:相对 3D 世界 0 海拔；1:相对 3D 世界地表；2:相对 3D 模型表面) 目前该属性仅支持属性值为 0        |
| data       | Array | 定义图层可视化数据                |
| data[0].id       | String |  模型型号对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                |
| data[0].label       | String |  标签文本                |
| data[0].coord       | Array |  XY 轴坐标，X：经度；Y：纬度                |
| data[0].coordZ       | Number |  Z 轴高度（单位：米）                |
| data[0].scale       | Array |  XYZ三轴的放缩倍数（单位：倍），不包含图标                |
| data[0].rotation       | Array |  XYZ 三轴旋转度数 [ 偏航角（单位：角度，0正北, -360 ~ 360）, 俯仰角（单位：角度，0水平, -360 ~ 360）,翻滚角（单位：角度，0水平, -360 ~ 360）]                |
| data[0].type       | String | 模型型号地标图例类别                |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 8.更新模型型号地标图样式

### 功能描述

更新模型型号地标图样式

### 调用方法

```js
let jsonData ={   
    id: "iconModelIdarkLayerId",
    alpha: 0.5,
    legends: [
        {
           name: "legendName1",
           iconModelId:"1111",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName2",
           iconModelId:"2222",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName3",
           iconModelId:"3333",
           labelScale: 1,
           iconScale: 1
        }
    ]
}



appInstance.uniCall('updateIconModelLandmarkLayerStyle', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号地标图层 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| alpha                | Number  | 透明度，0：完全透明；1：完全不透明                                                                                                   |
| legends                | Array  | 定义图层包含图例，支持为不同图例选取不同模型型号                                                                                                         |
| legends[0].name            | String  | 图层内图例名称，新建时调用者自己传入的唯一名称，用于各种操作的图例识别                                                                                                                         |
| legends[0].iconModelId           | String  |模型型号Id                                                                                                   |
| legends[0].labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| legends[0].iconScale       | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 9.删除模型型号地标图

### 功能描述

删除模型型号地标图

### 调用方法

```js
let jsonData = {
 id: 'iconModelLandmarkLayerId',   
 overlayType: 'IconModelLandmarkLayer',
};

appInstance.uniCall('removeOverlay', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性        | 类型   | 说明                                                                |
| ----------- | ------ | ------------------------------------------------------------------- |
| id          | String | 图层对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别 |
| overlayType | String | 图层对象类别                                                        |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |


## 10.添加模型型号轨迹图

### 功能描述

添加模型型号轨迹图

### 调用方法

```js
let jsonData ={   
    id: "iconModelIdLayerId",
    serverName:"serverName"，
    name: "layerName",
    coordType: 0,
    coordTypeZ: 0,
    snapSurface: 0,
    duration: 1,
    isAutoRotation: false,
    trackStyle: "style001",
    trackDuration: 50,
    trackWidth: 10,
    objLife: 500,
    visible: true,
    legends: [
        {
           name: "legendName1",
           iconModelId:"1111",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName2",
           iconModelId:"2222",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName3",
           iconModelId:"3333",
           labelScale: 1,
           iconScale: 1
        }
    ]
    data: [
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName2"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName3"
        }
    ]
}



appInstance.uniCall('addIconModelTrailLayer', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号轨迹对象Id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| serverName                   | String  |  连接的资产服务器的名称                                                                                                                    |
| name                   | String  |  图层名称，支持为图层自定义名称                                                                                                                    |
| coordType            | Number  | xy 轴坐标模式 坐标类型(0:x 经度 y 纬度；1:x 米 y 米，相对世界 00 点) 目前该属性仅支持属性值为 0                                      |
| coordTypeZ           | Number  | z 轴坐标模式 坐标高度类型，单位：米(0:相对 3D 世界 0 海拔；1:相对 3D 世界地表；2:相对 3D 模型表面) 目前该属性仅支持属性值为 0        |
| snapSurface                | Number   | 启用自动贴地,0：不贴地；1：贴地                                                                                                          |
| duration                | Number   | 从上一位置运动到新位置花费的时间（单位：秒），如果启用modelTrailData数据服务，同时表示同步数据的时间周期                                                                                                          |
| isAutoRotation                | Boolean  | 是否自动根据当前位置和上一位置计算运动方向，如果为true的话，data中的rotation将不起作用。    |
| trackStyle                | String  | 尾迹内置风格，详见注释    |
| trackDuration                | Number  | 尾迹粒子生命周期    |
| trackWidth                | Number  | 尾迹粒子的宽度    |
| objLife                | Number  | 批号消批时间长度                                                                                                   |
| visible                | Boolean  | 添加当前图层时默认显示还是隐藏                                                                                                   |
| legends                | Array  | 定义图层包含图例，支持为不同图例选取不同模型型号                                                                                                         |
| legends[0].name            | String  | 图层内图例名称，新建时调用者自己传入的唯一名称，用于各种操作的图例识别                                                                                                                         |
| legends[0].iconModelId           | String  |模型型号Id                                                                                                   |
| legends[0].labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| legends[0].iconScale       | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |
| data       | Array | 定义图层可视化数据                |
| data[0].id       | String |  图层子对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                |
| data[0].label       | String |  标签文本                |
| data[0].coord       | Array |  XY 轴坐标，X：经度；Y：纬度                |
| data[0].coordZ       | Number |  Z 轴高度（单位：米）                |
| data[0].scale       | Array |  XYZ三轴的放缩倍数（单位：倍），不包含图标                |
| data[0].rotation       | Array |  XYZ 三轴旋转度数 [ 偏航角（单位：角度，0正北, -360 ~ 360）, 俯仰角（单位：角度，0水平, -360 ~ 360）,翻滚角（单位：角度，0水平, -360 ~ 360）]                |
| data[0].type       | String | 模型型号地标图例类别                |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 11.更新模型型号轨迹图数据

### 功能描述

更新模型型号轨迹图数据

### 调用方法

```js
let jsonData ={   
    id: "iconModelIdLayerId",
    name: "layerName",
    coordType: 0,
    coordTypeZ: 0,
    isAppend: true,
    duration: 1,
    data: [
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName1"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName2"
        },
        {
            id: "objId",
            label: "poi title",
            coord: [134.55, 34.577],
            coordZ: 0,
            scale: [1, 1, 1],
            rotation: [20,80,90],
            type: "legendName3"
        }
    ]
}




appInstance.uniCall('updateIconModelTrailLayerCoord', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号轨迹对象Id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| name                   | String  |  图层名称，支持为图层自定义名称                                                                                                                    |
| coordType            | Number  | xy 轴坐标模式 坐标类型(0:x 经度 y 纬度；1:x 米 y 米，相对世界 00 点) 目前该属性仅支持属性值为 0                                      |
| coordTypeZ           | Number  | z 轴坐标模式 坐标高度类型，单位：米(0:相对 3D 世界 0 海拔；1:相对 3D 世界地表；2:相对 3D 模型表面) 目前该属性仅支持属性值为 0        |
| isAppend                | Boolean  | 是否追加数据（按数据顺序），true：按新数据 更新原有重复数据 & 追加新数据；false：清除原有数据 & 重建新数据    |
| duration                | Number   | 从上一位置运动到新位置花费的时间（单位：秒），如果启用modelTrailData数据服务，同时表示同步数据的时间周期                                                                                                          |
| data       | Array | 定义图层可视化数据                |
| data[0].id       | String |  图层子对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                |
| data[0].label       | String |  标签文本                |
| data[0].coord       | Array |  XY 轴坐标，X：经度；Y：纬度                |
| data[0].coordZ       | Number |  Z 轴高度（单位：米）                |
| data[0].scale       | Array |  XYZ三轴的放缩倍数（单位：倍），不包含图标                |
| data[0].rotation       | Array |  XYZ 三轴旋转度数 [ 偏航角（单位：角度，0正北, -360 ~ 360）, 俯仰角（单位：角度，0水平, -360 ~ 360）,翻滚角（单位：角度，0水平, -360 ~ 360）]                |
| data[0].type       | String | 模型型号地标图例类别                |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 12.更新模型型号轨迹图样式

### 功能描述

更新模型型号轨迹图样式

### 调用方法

```js
let jsonData ={   
    id: "iconModelIdLayerId",
    objLife: 500,
    isAutoRotation: false,
    trackStyle: "style001",
    trackDuration: 50,
    trackWidth: 10,
    legends: [
        {
           name: "legendName4",
            iconModelId:"4444",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName5",
            iconModelId:"5555",
           labelScale: 1,
           iconScale: 1
        },
        {
           name: "legendName6",
            iconModelId:"6666",
           labelScale: 1,
           iconScale: 1
        }
    ]
}




appInstance.uniCall('updateIconModelTrailLayerStyle', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性                 | 类型    | 说明                                                                                                                                 |
| -------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| id                   | String  |  模型型号轨迹对象Id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别                                                                                                                    |
| objLife                | Number  | 批号消批时间长度                                                                                                   |
| isAutoRotation                | Boolean  | 是否自动根据当前位置和上一位置计算运动方向，如果为true的话，data中的rotation将不起作用。    |
| trackStyle                | String  | 尾迹内置风格，详见注释    |
| trackDuration                | Number  | 尾迹粒子生命周期    |
| trackWidth                | Number  | 尾迹粒子的宽度    |
| legends                | Array  | 定义图层包含图例，支持为不同图例选取不同模型型号                                                                                                         |
| legends[0].name            | String  | 图层内图例名称，新建时调用者自己传入的唯一名称，用于各种操作的图例识别                                                                                                                         |
| legends[0].iconModelId           | String  |模型型号Id                                                                                                   |
| legends[0].labelScale | Number  | 文字标签的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                               |
| legends[0].iconScale       | Number | 图标的缩放，默认值是1表示原大，0 - 1：表示缩小倍率，大于1：表示放大倍率                                                                                                       |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 13.删除模型型号轨迹图

### 功能描述

模型型号轨迹图

### 调用方法

```js
let jsonData = {
  id: 'iconModelTrailLayerId', 
  overlayType: 'IconModelTrailLayer', 
};

appInstance.uniCall('removeOverlay', jsonData, (result) => {
  console.log(result);
});
```

#### 参数描述

| 属性        | 类型   | 说明                                                                |
| ----------- | ------ | ------------------------------------------------------------------- |
| id          | String | 图层对象 id，新建时调用者自己传入的唯一标识，用于各种操作的对象识别 |
| overlayType | String | 图层对象类别                                                        |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |
