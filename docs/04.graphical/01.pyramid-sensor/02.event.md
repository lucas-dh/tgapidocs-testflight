# 四棱锥体事件

## 目录

- [1.开始四棱锥体对象聚焦](#1开始四棱锥体对象聚焦)
- [2.结束四棱锥体对象聚焦](#2结束四棱锥体对象聚焦)

## 1.开始四棱锥体对象聚焦

### 功能描述

开始聚焦四棱锥体对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusPyramidSensorStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId"
}
```

#### 回调数据参数描述

| 属性 | 类型   | 说明    |
| ---- | ------ | ------- |
| id   | String | 模型 id |

[返回本章目录](#目录)

## 2.结束四棱锥体对象聚焦

### 功能描述

结束聚焦四棱锥体对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusPyramidSensorEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId"
}
```

#### 回调数据参数描述

| 属性 | 类型   | 说明    |
| ---- | ------ | ------- |
| id   | String | 模型 id |

[返回本章目录](#目录)
