# 事件图事件

## 目录

- [1.覆盖物单选事件](#1覆盖物单选事件)
- [2.开始事件图对象聚焦](#2开始事件图对象聚焦)
- [3.结束事件图对象聚焦](#3结束事件图对象聚焦)
- [4.开始事件图全部对象聚焦](#4开始事件图全部对象聚焦)
- [5.结束事件图全部对象聚焦](#5结束事件图全部对象聚焦)
- [6.开始事件图子对象聚焦](#6开始事件图子对象聚焦)
- [7.结束事件图子对象聚焦](#7结束事件图子对象聚焦)
- [8.开始事件图图例聚焦](#8开始事件图图例聚焦)
- [9.结束事件图图例聚焦](#9结束事件图图例聚焦)
- [10.鼠标悬停事件图对象](#10鼠标悬停事件图对象)
- [11.鼠标离开事件图对象](#11鼠标离开事件图对象)
- [12.事件图左键点击事件回调](#12事件图左键点击事件回调)

## 1.覆盖物单选事件

### 功能描述

开始覆盖物单选事件

### 调用方法

```js
appInstance.uniCall(
  'pickOverlay',
  {
    overlayType: 'eventLayer',
    idLayer: '0',
    type: 'click',
    isShowDecorator: true,
  },
  (result) => {
    console.log(result);
  }
);
```

#### 参数描述

| 属性            | 类型    | 说明                                                                |
| --------------- | ------- | ------------------------------------------------------------------- |
| overlayType     | String  | 可选中图层对象类别                                                  |
| idLayer         | String  | 填写图层对象 id，如不限制，可不填                                   |
| type            | String  | 单选模式，click：点击选中；hover：悬停选中                          |
| isShowDecorator | Boolean | 表示是否显示选中的高亮装饰符，true：显示，false：不显示，默认：true |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 2.开始事件图对象聚焦

### 功能描述

开始事件图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明        |
| ------- | ------ | ----------- |
| idLayer | String | 事件图层 id |

[返回本章目录](#目录)

## 3.结束事件图对象聚焦

### 功能描述

结束事件图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明        |
| ------- | ------ | ----------- |
| idLayer | String | 事件图层 id |

[返回本章目录](#目录)

## 4.开始事件图全部对象聚焦

### 功能描述

开始事件图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllEventLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 5.结束事件图全部对象聚焦

### 功能描述

结束事件图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllEventLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 6.开始事件图子对象聚焦

### 功能描述

开始事件图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerObjStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idObj   | String | 事件图层名称 |
| idLayer | String | 事件图层 id  |

[返回本章目录](#目录)

## 7.结束事件图子对象聚焦

### 功能描述

结束事件图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerObjEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idObj   | String | 事件图层名称 |
| idLayer | String | 事件图层 id  |

[返回本章目录](#目录)

## 8.开始事件图图例聚焦

### 功能描述

开始事件图图例聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerLegendStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId",
  "legend": "legendName"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idLayer | String | 事件图层 id  |
| legend  | String | 事件图层图例 |

[返回本章目录](#目录)

## 9.结束事件图图例聚焦

### 功能描述

结束事件图图例聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusEventLayerLegendEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId",
  "legend": "legendName"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idLayer | String | 事件图层 id  |
| legend  | String | 事件图层图例 |

[返回本章目录](#目录)

## 10.鼠标悬停事件图对象

### 功能描述

鼠标悬停事件图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onEventLayerHover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性  | 类型   | 说明                                 |
| ----- | ------ | ------------------------------------ |
| idObj | String | 鼠标进入路径点对象时触发此事件       |
| coord | Array  | 鼠标进入路径点对象时的第一个交点位置 |

[返回本章目录](#目录)

## 11.鼠标离开事件图对象

### 功能描述

鼠标离开事件图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onEventLayerUnhover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                           |
| ------- | ------ | ------------------------------ |
| idObj   | String | 鼠标离开路径点对象时触发此事件 |
| idLayer | String | 鼠标离开路径点对象时触发此事件 |

[返回本章目录](#目录)

## 12.事件图左键点击事件回调

### 功能描述

获取点击事件图对象信息

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onEventLayerClick',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId",
  "selected": false
}
```

#### 回调数据参数描述

| 属性     | 类型    | 说明                           |
| -------- | ------- | ------------------------------ |
| idObj    | String  | 鼠标左键点击路径点对象时触发此事件 |
| idLayer  | String  | 事件图层 id                    |
| selected | Boolean | 事件图层对象当前选中状态       |

[返回本章目录](#目录)
