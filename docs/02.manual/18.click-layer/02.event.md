# 点迹图事件

## 目录

- [1.覆盖物单选事件](#1覆盖物单选事件)
- [2.开始点迹图对象聚焦](#2开始点迹图对象聚焦)
- [3.结束点迹图对象聚焦](#3结束点迹图对象聚焦)
- [4.开始点迹图全部对象聚焦](#4开始点迹图全部对象聚焦)
- [5.结束点迹图全部对象聚焦](#5结束点迹图全部对象聚焦)
- [6.开始点迹图子对象聚焦](#6开始点迹图子对象聚焦)
- [7.结束点迹图子对象聚焦](#7结束点迹图子对象聚焦)
- [8.开始点迹图图例聚焦](#8开始点迹图图例聚焦)
- [9.结束点迹图图例聚焦](#9结束点迹图图例聚焦)
- [10.鼠标悬停点迹图对象](#10鼠标悬停点迹图对象)
- [11.鼠标离开点迹图对象](#11鼠标离开点迹图对象)
- [12.左键点击点迹图对象](#12左键点击点迹图对象)

## 1.覆盖物单选事件

### 功能描述

开始覆盖物单选事件

### 调用方法

```js
appInstance.uniCall(
  'pickOverlay',
  {
    overlayType: 'trackLayer',
    idLayer: '0',
    type: 'click',
    isShowDecorator: true,
  },
  (result) => {
    console.log(result);
  }
);
```

#### 参数描述

| 属性            | 类型    | 说明                                                                |
| --------------- | ------- | ------------------------------------------------------------------- |
| overlayType     | String  | 可选中图层对象类别                                                  |
| idLayer         | String  | 填写图层对象 id，如不限制，可不填                                   |
| type            | String  | 单选模式，click：点击选中；hover：悬停选中                          |
| isShowDecorator | Boolean | 表示是否显示选中的高亮装饰符，true：显示，false：不显示，默认：true |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 2.开始点迹图对象聚焦

### 功能描述

开始点迹图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明        |
| ------- | ------ | ----------- |
| idLayer | String | 点迹图层 id |

[返回本章目录](#目录)

## 3.结束点迹图对象聚焦

### 功能描述

结束点迹图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明        |
| ------- | ------ | ----------- |
| idLayer | String | 点迹图层 id |

[返回本章目录](#目录)

## 4.开始点迹图全部对象聚焦

### 功能描述

开始点迹图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllTrackLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 5.结束点迹图全部对象聚焦

### 功能描述

结束点迹图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllTrackLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 6.开始点迹图子对象聚焦

### 功能描述

开始点迹图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerObjStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idObj   | String | 点迹图层名称 |
| idLayer | String | 点迹图层 id  |

[返回本章目录](#目录)

## 7.结束点迹图子对象聚焦

### 功能描述

结束点迹图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerObjEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idObj   | String | 点迹图层名称 |
| idLayer | String | 点迹图层 id  |

[返回本章目录](#目录)

## 8.开始点迹图图例聚焦

### 功能描述

开始点迹图图例聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerLegendStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId",
  "legend": "legendName"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idLayer | String | 点迹图层 id  |
| legend  | String | 点迹图层图例 |

[返回本章目录](#目录)

## 9.结束点迹图图例聚焦

### 功能描述

结束点迹图图例聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusTrackLayerLegendEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId",
  "legend": "legendName"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明         |
| ------- | ------ | ------------ |
| idLayer | String | 点迹图层 id  |
| legend  | String | 点迹图层图例 |

[返回本章目录](#目录)

## 10.鼠标悬停点迹图对象

### 功能描述

鼠标悬停点迹图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onTrackLayerHover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性  | 类型   | 说明                                 |
| ----- | ------ | ------------------------------------ |
| idObj | String | 鼠标进入路径点对象时触发此事件       |
| coord | Array  | 鼠标进入路径点对象时的第一个交点位置 |

[返回本章目录](#目录)

## 11.鼠标离开点迹图对象

### 功能描述

鼠标离开点迹图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onTrackLayerUnhover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                           |
| ------- | ------ | ------------------------------ |
| idObj   | String | 鼠标离开路径点对象时触发此事件 |
| idLayer | String | 鼠标离开路径点对象时触发此事件 |

[返回本章目录](#目录)

## 12.左键点击点迹图对象

### 功能描述

左键点击点迹图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onTrackLayerClick',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId",
  "selected": false
}
```

#### 回调数据参数描述

| 属性     | 类型    | 说明                           |
| -------- | ------- | ------------------------------ |
| idObj    | String  | 鼠标离开路径点对象时触发此事件 |
| idLayer  | String  | 点迹图层 id                    |
| selected | Boolean | 点迹图层对象当前选中状态       |

[返回本章目录](#目录)
