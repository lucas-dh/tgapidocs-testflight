# 模型地标图事件

## 目录

- [1.覆盖物单选事件](#1覆盖物单选事件)
- [2.开始模型地标图对象聚焦](#2开始模型地标图对象聚焦)
- [3.结束模型地标图对象聚焦](#3结束模型地标图对象聚焦)
- [4.开始模型地标图全部对象聚焦](#4开始模型地标图全部对象聚焦)
- [5.结束模型地标图全部对象聚焦](#5结束模型地标图全部对象聚焦)
- [6.开始模型地标图子对象聚焦](#6开始模型地标图子对象聚焦)
- [7.结束模型地标图子对象聚焦](#7结束模型地标图子对象聚焦)
- [8.鼠标悬停模型地标图对象](#8鼠标悬停模型地标图对象)
- [9.鼠标离开模型地标图对象](#9鼠标离开模型地标图对象)
- [10.左键点击模型地标图对象](#10左键点击模型地标图对象)

## 1.覆盖物单选事件

### 功能描述

开始覆盖物单选事件

### 调用方法

```js
appInstance.uniCall(
  'pickOverlay',
  {
    overlayType: 'modelLandmarkLayer',
    idLayer: '0',
    type: 'click',
    isShowDecorator: true,
  },
  (result) => {
    console.log(result);
  }
);
```

#### 参数描述

| 属性            | 类型    | 说明                                                                |
| --------------- | ------- | ------------------------------------------------------------------- |
| overlayType     | String  | 可选中图层对象类别                                                  |
| idLayer         | String  | 填写图层对象 id，如不限制，可不填                                   |
| type            | String  | 单选模式，click：点击选中；hover：悬停选中                          |
| isShowDecorator | Boolean | 表示是否显示选中的高亮装饰符，true：显示，false：不显示，默认：true |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 2.开始模型地标图对象聚焦

### 功能描述

开始模型地标图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusModelLandmarkLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明            |
| ------- | ------ | --------------- |
| idLayer | String | 模型地标图层 id |

[返回本章目录](#目录)

## 3.结束模型地标图对象聚焦

### 功能描述

结束模型地标图对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusModelLandmarkLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明            |
| ------- | ------ | --------------- |
| idLayer | String | 模型地标图层 id |

[返回本章目录](#目录)

## 4.开始模型地标图全部对象聚焦

### 功能描述

开始模型地标图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllModelLandmarkLayerStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 5.结束模型地标图全部对象聚焦

### 功能描述

结束模型地标图全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllModelLandmarkLayerEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 6.开始模型地标图子对象聚焦

### 功能描述

开始模型地标图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusModelLandmarkLayerObjStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明             |
| ------- | ------ | ---------------- |
| idObj   | String | 模型地标图层名称 |
| idLayer | String | 模型地标图层 id  |

[返回本章目录](#目录)

## 7.结束模型地标图子对象聚焦

### 功能描述

结束模型地标图子对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusModelLandmarkLayerObjEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性     | 类型   | 说明             |
| -------- | ------ | ---------------- |
| nameArea | String | 模型地标图层名称 |
| idLayer  | String | 模型地标图层 id  |

[返回本章目录](#目录)

## 8.鼠标悬停模型地标图对象

### 功能描述

鼠标悬停模型地标图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onModelLandmarkLayerHover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明             |
| ------- | ------ | ---------------- |
| idObj   | String | 模型地标图层名称 |
| idLayer | String | 模型地标图层 id  |

[返回本章目录](#目录)

## 9.鼠标离开模型地标图对象

### 功能描述

鼠标离开模型地标图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onModelLandmarkLayerUnhover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明             |
| ------- | ------ | ---------------- |
| idObj   | String | 模型地标图层名称 |
| idLayer | String | 模型地标图层 id  |

[返回本章目录](#目录)

## 10.左键点击模型地标图对象

### 功能描述

左键点击模型地标图对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onModelLandmarkLayerClick',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "idObj": "objId",
  "idLayer": "layerId",
  "selected": false
}
```

#### 回调数据参数描述

| 属性     | 类型    | 说明                         |
| -------- | ------- | ---------------------------- |
| idObj    | String  | 模型地标图层名称             |
| idLayer  | String  | 模型地标图层 id              |
| selected | Boolean | 模型地标图层对象当前选中状态 |

[返回本章目录](#目录)
