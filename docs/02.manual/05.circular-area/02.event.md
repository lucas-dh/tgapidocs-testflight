# 圆形区域事件

## 目录

- [1.覆盖物单选事件](#1覆盖物单选事件)
- [2.圆形区域图层左键点击事件回调](#2圆形区域图层左键点击事件回调)
- [3.开始圆形轮廓对象聚焦](#3开始圆形轮廓对象聚焦)
- [4.结束圆形轮廓对象聚焦](#4结束圆形轮廓对象聚焦)
- [5.开始圆形轮廓全部对象聚焦](#5开始圆形轮廓全部对象聚焦)
- [6.结束圆形轮廓全部对象聚焦](#6结束圆形轮廓全部对象聚焦)
- [7.鼠标悬停圆形轮廓对象](#7鼠标悬停圆形轮廓对象)
- [8.鼠标离开圆形轮廓对象](#8鼠标离开圆形轮廓对象)

## 1.覆盖物单选事件

### 功能描述

开始覆盖物单选事件

### 调用方法

```js
appInstance.uniCall(
  'pickOverlay',
  {
    overlayType: 'circularArea',
    idLayer: '0',
    type: 'click',
    isShowDecorator: true,
  },
  (result) => {
    console.log(result);
  }
);
```

#### 参数描述

| 属性            | 类型    | 说明                                                                |
| --------------- | ------- | ------------------------------------------------------------------- |
| overlayType     | String  | 可选中图层对象类别                                                  |
| idLayer         | String  | 填写图层对象 id，如不限制，可不填                                   |
| type            | Number  | 单选模式，click：点击选中；hover：悬停选中                          |
| isShowDecorator | Boolean | 表示是否显示选中的高亮装饰符，true：显示，false：不显示，默认：true |

#### 回调数据

```json
{
  "result": 1,
  "message": "成功。"
}
```

#### 回调数据参数描述

| 属性    | 类型   | 说明                     |
| ------- | ------ | ------------------------ |
| result  | Number | 1：代表成功；0：代表失败 |
| message | String | 成功或者失败原因         |

[返回本章目录](#目录)

## 2.圆形区域图层左键点击事件回调

### 功能描述

获取点击圆形区域图层对象信息

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onCircularAreaClick',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId",
  "selected": false,
  "tag": "customerinfo",
  "coord": [113.320381, 23.108027]
}
```

#### 回调数据参数描述

| 属性     | 类型     | 说明                             |
| -------- | -------- | -------------------------------- |
| id       | String   | 圆形区域图层 id                  |
| selected | Boolean  | 圆形区域图层对象当前选中状态     |
| tag      | Anything | 用户自定义属性                   |
| coord    | String   | 鼠标点击圆形区域对象时的鼠标位置 |

[返回本章目录](#目录)

## 3.开始圆形轮廓对象聚焦

### 功能描述

开始圆形轮廓对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusCircularAreaStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId"
}
```

#### 回调数据参数描述

| 属性 | 类型   | 说明            |
| ---- | ------ | --------------- |
| id   | String | 圆形区域图层 id |

[返回本章目录](#目录)

## 4.结束圆形轮廓对象聚焦

### 功能描述

结束圆形轮廓对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusCircularAreaEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId"
}
```

#### 回调数据参数描述

| 属性 | 类型   | 说明            |
| ---- | ------ | --------------- |
| id   | String | 圆形区域图层 id |

[返回本章目录](#目录)

## 5.开始圆形轮廓全部对象聚焦

### 功能描述

开始圆形轮廓全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllCircularAreaStart',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 6.结束圆形轮廓全部对象聚焦

### 功能描述

结束圆形轮廓全部对象聚焦

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onFocusAllCircularAreaEnd',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

[返回本章目录](#目录)

## 7.鼠标悬停圆形轮廓对象

### 功能描述

鼠标悬停圆形轮廓对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onCircularAreaHover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId",
  "tag": "customerinfo",
  "coord": [113.320381, 23.108027]
}
```

#### 回调数据参数描述

| 属性  | 类型     | 说明                                   |
| ----- | -------- | -------------------------------------- |
| id    | String   | 圆形区域图层 id                        |
| tag   | Anything | 用户自定义属性                         |
| coord | String   | 鼠标进入圆形区域对象时的第一个交点位置 |

[返回本章目录](#目录)

## 8.鼠标离开圆形轮廓对象

### 功能描述

鼠标离开圆形轮廓对象

### 调用方法

```js
appInstance.uniCall(
  'addEventListener',
  {
    eventName: 'onCircularAreaUnhover',
    callback: function (event) {
      console.log(event);
    },
  },
  (result) => {
    console.log(result);
  }
);
```

#### 回调数据

```json
{
  "id": "objId",
  "tag": "customerinfo"
}
```

#### 回调数据参数描述

| 属性 | 类型     | 说明            |
| ---- | -------- | --------------- |
| id   | String   | 圆形区域图层 id |
| tag  | Anything | 用户自定义属性  |

[返回本章目录](#目录)
